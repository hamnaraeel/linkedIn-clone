import styled from "styled-components";


const Main  = (props) => {
    return (
        <Container> 
            main
        </Container>
    );
};

const Container = styled.div``;

export default Main;