import styled from "styled-components";
import Header from "./Header";


const Home = (props) => {
    return (
        <div>height
            <Header />
        </div>
    );
};




export default Home